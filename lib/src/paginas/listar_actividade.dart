import 'package:flutter/material.dart';
import 'package:splash_screen/src/paginas/menu.dart';
import '/src/componentes/custom_text_field.dart';


class ListarActividades extends StatelessWidget {
  const ListarActividades({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

      appBar: AppBar(
        title: Text('        LISTAR TAREFAS'),
      ),
      backgroundColor: Colors.white,
      body:  Column(
        children: [



        ],
      ),
    );


  }
}
